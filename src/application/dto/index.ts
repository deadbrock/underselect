/**
 * DTOs (Data Transfer Objects) — contratos de entrada/saída dos use cases.
 * Serão definidos conforme funcionalidades forem implementadas.
 */

export interface BaseDTO {
  readonly id?: string;
}
